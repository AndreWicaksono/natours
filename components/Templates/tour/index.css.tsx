import styled from "@emotion/styled";

import { cssTemplatePageContainer } from "global/variables";

const TemplatePageTourIndexBase = styled.div`
  ${cssTemplatePageContainer}
`;

export default TemplatePageTourIndexBase;
